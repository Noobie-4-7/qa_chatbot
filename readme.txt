1.) Create a .env file and add the OPENAI_API_KEY for running openai chat and add the GEMINI_API_KEY for 
running the gemini chat (which is free)
2.) Run command streamlit run app_gemini.py to open the UI on localhost.